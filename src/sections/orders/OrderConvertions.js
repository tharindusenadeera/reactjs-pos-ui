import React from 'react';

/**
 * todo: Order fomatting should be done here
 * @param {*} param
 * @returns formatted item list array
 */
export const getFormattedOrder = ({ order }) => {
    const formattedOrder = order;
    
    return formattedOrder;
}
